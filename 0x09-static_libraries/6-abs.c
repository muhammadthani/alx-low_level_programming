#include "main.h"
/**
* _abs - returns the absolute value of an int
* @c: parameter definition
* Return: c in absolute value
*/
int _abs(int c)
{
	if (c >= 0)
	{
		return (c);
	}
	else
	{
		return (-c);
	}
}
